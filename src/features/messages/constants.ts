// Shared by ChatPanel (renders at these sizes) and ChatWindowsManager
// (uses the same numbers to work out how many panels actually fit on
// screen) — one definition so they can't drift apart.
export const PANEL_WIDTH = 328;
export const PANEL_GAP = 12;
export const BUBBLE_SIZE = 56;
export const BUBBLE_GAP = 12;
export const EDGE_OFFSET = 16;
